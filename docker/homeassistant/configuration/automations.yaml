alias: Trigger when downstairs boiler changes state
trigger:
  platform: state
  entity_id: climate.downstairs
action:
  service: script.notify_mqtt
  data_template:
    zone: downstairs
    mode: {{ trigger.to_state.attributes.hvac_action }} # heating, idle